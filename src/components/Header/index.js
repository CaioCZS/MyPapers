import { ContainerHeader, MenuIcon, CartIcon } from "./style";

export default function Header() {
  return (
    <ContainerHeader>
      <MenuIcon />
      <h1>LOGO</h1>
      <CartIcon />
    </ContainerHeader>
  );
}
